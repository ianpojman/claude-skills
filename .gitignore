# Runtime data (don't track)
history.jsonl
debug/
file-history/
session-env/
shell-snapshots/
todos/
statsig/
projects/
tasks/
plans/
local/
plugins/
ide/
# Note: commands/ is tracked (slash commands)

# Symlink to skills/scripts (root level only)
/scripts

# Skill script directories (but not skill symlinks)
skills/scripts/

# TaskFlow v2 local cache
tasks/v2/.taskflow.db

# TaskFlow session state (per-machine)
.taskflow-session.json
